



function filter1(start: Int, end: Int): Set[Int]

field val: Int

method test1(i1: Int) {
    var a: Bool := true

    var b: Int := comp[and(),0]{identityMap(anyReceiver().val, 1) | filter1(0,10)}

    assert(b == 5)
}
